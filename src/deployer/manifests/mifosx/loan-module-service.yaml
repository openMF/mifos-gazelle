apiVersion: v1
kind: Service
metadata:
  annotations:
    description: "Service for the Mifos reactive loan risk-assessment module"
  labels:
    app: loan-module
    tier: backend
  name: loan-module
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: loan-module
